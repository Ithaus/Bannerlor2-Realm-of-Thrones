# OTWARTE SPRAWY — stan na 2026-09-01

Zrodlo: przeglad wielo-agentowy (29 agentow) po AUDYCIE-2026-08-31.
Kazda pozycja byla WERYFIKOWANA w kodzie — zostaly tylko te z dowodem,
ze nadal sa otwarte. Trzy pozycje odpadly jako juz zrobione.

## A. Bledy do naprawienia kodem

1. **DTE: AmbiguousMatchException na ticku paska mapy** (krytyczne)
   MapArmoryReadinessMixin (DynamicTroopEquipmentReupload) -> UIExtenderEx
   BaseViewModelMixin<MapBarVM> cctor -> AccessTools2.Method -> Type.GetMethod
   pada na 1.4.8. Cctor pada raz, potem KAZDE odswiezenie MapBarVM.Tick rzuca
   ponownie. Kandydat na latke w CrashScribe/Mends.

2. **Rozjazd dokumentacji z kodem: cap dzienny dezercji AI NIE ISTNIEJE**
   CHANGELOG (l. 360-361) obiecuje "AI-safety: polowa stawki + cap dzienny".
   W WarLedger.cs:57-58 jest tylko polowa stawki; `pct` rosnie liniowo z dniami
   zwloki (po 20 dniach AI traci 4.5%/dzien), a DesertElitesFirst nie ma podlogi
   liczebnosci — partia tla moze zejsc do samego lorda. Do decyzji: clamp
   (np. max 2%/dzien dla AI) + minimalna liczebnosc partii.

3. **Suwak MCM "Fence Price Multiplier" (RealisticCaptivity) nic nie robi**
   Pelna oprzewodowka do gracza (MCM 0.00-4.00, zapis do XML), ale ZADEN kod
   go nie czyta. Albo wdrozyc, albo usunac z MCM.

4. **Suwak "AiNightsAwakeInChase" (Armoury) bez implementacji**
   Opis obiecuje limit nocy pogoni bez snu; w NightRest.cs nie ma odczytu.

5. **Zamowienia lordow nie znaja IsOpened** — moga zadac czesci, ktorych gracz
   nie odblokowal. Po zacisnieciu bramy kucia do T1 ryzyko wieksze niz przed.

6. **Cross-modowe czytanie ustawien po stringu** (CrashScribe -> Armoury.Settings:
   KgPerAthleticsPoint, ForgePartsFreeBelowTier) z pustym `catch {}` — zmiana
   nazwy pola da CICHA awarie z fallbackiem. Do rozwazenia: log przy nietrafieniu.

## B. Dane ROT (kod tego nie naprawi)

7. **OneHandedSwordDual**: cala pula kling na T4-T5, wiec podloga bramy zostawia
   darmowe 32 klingi T4. Trzeba dolozyc nizsze klingi w danych szablonu.
8. **ROT_ThrowingAxe.Handle**: zaden tier nie ma czesci domyslnej — przycisk Kuj
   dla toporow miotanych prawdopodobnie nie dziala W OGOLE (bylo zepsute przed
   nasza brama). Potwierdzic w grze i zglosic/zalatac danymi.

## C. Do potwierdzenia w grze przez gracza

9. Przycisk **Kuj swieci mimo bramy T1** (LoreForgeGate) — otworzyc szablony,
   sprawdzic czy zaden slot nie zostal bez darmowej czesci.
10. W logu CrashScribe poszukac linii "Mends: brama kucia - prog T2..." i czy
    pojawil sie czlon "SAMOKONTROLA przywrocila" (= dane ROT mialy slot bez pokrycia).
11. Z sesji 29-30.08 niezweryfikowane: panel wyniku kucia (dzwiek + Done bez
    crasha), ksiega stanow magazynu przez save (arm_armory_wear), log "AWANS ... magazyn".

## D. Sprzecznosc w dokumentacji — do rozstrzygniecia

12. **Namiot obozu / obozowanie bandytow / dlug snu**: HISTORY.md mowi
    ODBUDOWANE 26-27.08 i dziala; ERRORS.md (30.08) opisuje te same rzeczy jako
    cofniete "do zrobienia na nowo". Jeden z plikow klamie — sprawdzic w kodzie
    i poprawic dokumentacje.

## E. Pokretla, ktorych Jeff jeszcze nie przestawil

13. SmithingSkillPerTier 45 -> 35
14. DurabilityPerArmorPoint 20 -> 6-8
15. MinSellPercentOfValue 5 -> 2-3
16. Tempa marszu (wartosci docelowe nieustalone)

## F. Do obserwacji w pierwszych tygodniach kampanii (strojenie, nie bledy)

17. Tempo dezercji AI przy biedzie lordow BK (suwak WagesDesertPercentPerDay,
    domyslnie 0.5) — armie tla nie moga stopniec.
18. Zimowa kaskada glodu AI; czy jesienny bufor x2 wystarcza
    (AutumnStockMultiplier, domyslnie 2.0).
    **Ustalenie z dekompilacji BK:** PartySupplies.PostInitialize jest wolane
    tylko w konstruktorze i OnGameLoaded, a nasz postfix wylacznie OBNIZA —
    wiec jesienny cap x2 przykleja sie do partii az do przeladowania save'a,
    a partia zalozona zima nie dostaje bufora wcale. Warto przeliczac cap
    na dziennym ticku.
19. Wydajnosc petli dziennych: foraging ScorchedEarth (~150 lordow x ~400 wiosek)
    i skan partii pod namioty co ~2 s (cap 60).
20. Czy skalowac leczenie rannych oraz starzenie/ciaze do zwolnionego swiata
    (WorldPace) — dzis licza sie w dniach vanilla.

## Zamkniete tym przegladem
- Watcher wgral wszystkie DLL: **potwierdzone 01.09** — piec modow zgodnych
  co do bajta z buildem (cmp).
- Dwie pozycje z audytu okazaly sie juz zrealizowane w kodzie.

## Martwy kod (do sprzatniecia lub swiadomego zostawienia)
- `CrashScribe/src/Sampler.cs` — 141 linii, wywolanie zakomentowane (profiler
  zawieszal gre; powod opisany rzetelnie). Zostawic z komentarzem albo usunac.
- `QuartermasterLaw.Thresholds()` + pole `_fThresholds` — rozwiazywane refleksja
  przy starcie, nigdy nieuzywane.
- `ArmouryBehavior.RefundSeizedAmmo()` — wylaczone 30.08 (Jeff wyjasnil, ze wklad
  MA przechodzic do wojska).
